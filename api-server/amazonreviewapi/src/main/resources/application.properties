spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=amazon

# Bind to all network interfaces so external access via IP is possible
server.address=0.0.0.0
server.port=8080
